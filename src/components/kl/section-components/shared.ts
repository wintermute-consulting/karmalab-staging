import type { CSSProperties } from 'react';

/** Static Tailwind classes for all section wrappers. */
export const sectionWrapClass =
  'relative z-[4] bg-kl-black max-w-[1400px] mx-auto w-full py-16 sm:py-24 px-[clamp(24px,5vw,72px)]';
